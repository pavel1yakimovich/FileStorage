@using MVCUI.ViewModels.File
@model MVCUI.ViewModels.IndexViewModel<FileViewModel>
@using MVCUI.Helpers

@{
    ViewBag.Title = "Files";
}

<div id="allItems">
    <h2>All files</h2>
    @Html.Partial("_FileTable", Model.Items)

    <div class="btn-group content-pager">
        @Html.PageLinks(Model.PageInfo, x => Ajax.ActionLink(x.ToString(), "All", "File", new { page = x }, new AjaxOptions { UpdateTargetId = "allItems" }).ToString())
    </div>
</div>
@section Scripts
{
    <script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
}