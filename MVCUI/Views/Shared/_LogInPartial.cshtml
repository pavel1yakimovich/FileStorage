@*User.Identity.IsAuthenticated*@
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-left" }))
    {
        @Html.AntiForgeryToken()
        
            <li class="w3-hide-small">
                @Html.ActionLink("Your profile, " + User.Identity.Name, "Profile", "Account", new { name = User.Identity.Name }, new { @class="w3-hover-white" })
            </li>
            <li class="w3-hide-small">
                @Html.ActionLink("Upload new file", "Create", "File", routeValues: null, htmlAttributes: new { @class = "w3-hover-white" })
            </li>
            if (ViewContext.HttpContext.User.IsInRole("Admin"))
            {
                <li class="w3-hide-small">@Html.ActionLink("All users", "Index", "Account", routeValues: null, htmlAttributes: new { @class = "w3-hover-white" })</li>
            }
            <li class="w3-hide-small">@Html.ActionLink("Log off", "LogOff", "Account", routeValues: null, htmlAttributes: new { @class = "w3-hover-white" })</li>
    }
}
else
{
        <li class="w3-hide-small">@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink", @class="w3-hover-white" })</li>
        <li class="w3-hide-small">@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink", @class = "w3-hover-white" })</li>
}
